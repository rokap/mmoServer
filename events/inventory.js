var utilities = require('../libs/utilities');

module.exports = function (io, socket, db) {

    var inventory = {};

    // inventory events coming soon
    utilities.debug(socket, "Inventory Events");

    return this;

};