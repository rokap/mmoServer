var utilities = require('../libs/utilities');

module.exports = function (io, socket, db) {

    var zone = {};

    // zone events coming soon
    utilities.debug(socket, "Zone Events");

    return this;

};