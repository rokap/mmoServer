var utilities = require('../libs/utilities');

module.exports = function (io, socket, db) {

    var trade = {};

    // trade events coming soon
    utilities.debug(socket, "Trade Events");

    return this;

};