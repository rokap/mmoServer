var utilities = require('../libs/utilities');

module.exports = function (io, socket, db) {

    var npc = {};

    // npc events coming soon
    utilities.debug(socket, "NPC Events");

    return this;

};