var utilities = require('../libs/utilities');

module.exports = function (io, socket, db) {

    var quest = {};

    // quest events coming soon
    utilities.debug(socket, "Quest Events");

    return this;

};